# v1.4.1 Multi-Account Runtime & Provider Foundation 发布检查清单

- [x] Research Gate、Community Architecture Research Matrix、官方协议/认证与许可证评估完成
- [x] account_id 驱动 Runtime Router，Gmail 收件、QQ GUI 发件、历史补扫和连接测试按账号路由
- [x] Account create/read/update、enable/disable、soft remove 可用；历史 ownership 默认保留
- [x] Credential Manager key 按 account_id 隔离；旧 key 只允许精确匹配兼容账号迁移
- [x] Gmail OAuth credentials/Token/锁按 account_id 隔离；清除 Token 不从旧文件重新恢复
- [x] 每账号同步状态、线程/进程锁、retry/backoff 与未分类异常隔离自动化通过
- [x] 全局协调器状态不会覆盖账号 last result、failure count 或 backoff
- [x] 同 Provider 多账号的 ID、配置、Credential、OAuth、状态和 ownership 隔离自动化通过
- [x] Generic IMAP/SMTP TLS-only 配置、SMTP 只认证、目录发现、SPECIAL-USE 与 UID checkpoint 基础通过
- [x] Gmail scope 仍严格且唯一为 gmail.readonly；Gmail send 明确未实现，未扩大权限
- [x] 一个 MCP、七工具、固定 OWNER_GMAIL、路径白名单、UTF-8/stdout purity 与 Hash 边界保持
- [x] GUI 真实新增账号、动态卡、启停、OAuth、连接测试、目录发现、软移除和收发账号选择完成
- [x] 最终冻结代码全量 pytest：555 passed、1 skipped，1731.28 秒
- [x] compileall、pip check、版本命令、diff check 和 115 项重点回归通过
- [x] v1.4 schema 1→v1.4.1 schema 2 隔离迁移、备份、raw.eml、中文附件、OAuth 与 Hash 保全通过
- [x] v1.4.0→v1.4.1 portable overlay：旧/新 GUI self-test 与新版 MCP smoke 通过
- [x] clean build、GUI packaged self-test、MCP 七工具 smoke、ZIP、installer、checksums 完成
- [x] dist/release/ZIP secret scan：287 个文件；tracked forbidden-file scan：154 个路径
- [x] IMAPClient 3.1.0 New BSD metadata、AUTHORS 与 COPYING 已进入 Windows 包
- [x] Defender release/dist CustomScan 完成；历史检测总数前后均为 3，无新增
- [ ] 真实已安装 v1.3/v1.4→v1.4.1 覆盖安装、真实用户库摘要与卸载保留：缺少隔离 Windows VM/测试用户，NOT_TESTED
- [ ] 真实第二 Gmail/QQ、Generic 服务器、多账号 OAuth/收发网络 E2E：无独立测试凭据，NOT_TESTED
- [ ] 人工 100%/125%/150% 深浅色六档截图：自动化通过，人工截图 PARTIAL
- [ ] Authenticode：GUI、MCP、Setup 三个 EXE 均为 NotSigned，当前无签名证书
- [x] 实现与验收文档 commits 已正常推送 origin/master；最终状态补记继续正常推送

未获得用户明确批准，不得创建 Tag、GitHub Release 或公开 Release Assets。
