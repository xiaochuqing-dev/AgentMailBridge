# v1.4.0 Multi-Account Core 发布检查清单

- [x] 顶部仅收件、发件；侧栏统一邮箱账号列表与 Agent/MCP、历史记录、文件与数据、设置、关于
- [x] Gmail/QQ 旧配置映射为稳定 MailAccount；Provider Adapter 只声明真实已接通能力
- [x] package、兼容收件、同步、重试、规则评估、outbound 和 sent 事实带账号归属
- [x] 多账号 ID、同类型账号、同步、同 Message-ID package、outbound 隔离自动化通过
- [x] 收件切换为一封邮件一行，完成邮件详情、会话和资源自然展示
- [x] 历史记录按邮件展示，Files & Data 可查看所属邮件并双向导航
- [x] 发件支持正文、0 至多个附件、0 至多个链接，一次确认只发送一封 MIME 邮件
- [x] Agent / MCP 位于发件标题区，交付指令无需用户填写路径
- [x] Agent 工作区授权持久化且拒绝过宽、系统和敏感目录
- [x] Gmail scope、固定收件人、MCP staging/Hash、Credential Manager 和自动收件边界未削弱
- [x] 完整 pytest 首轮 534 passed、1 skipped；唯一 Windows 临时库句柄失败修复后该用例 1 passed
- [x] 模拟 v1.3 数据迁移、回滚、幂等、raw.eml 与 Hash 保全
- [x] Git 与现有构建产物敏感信息扫描
- [x] 100%、125%、150% 布局、normal/maximize/restore/manual resize 自动化回归
- [x] dist 与 ZIP 的 GUI self-test、MCP 七工具、UTF-8/BOM、EOF 和 Hash smoke
- [x] clean build、packaged self-test、MCP smoke、ZIP、installer、哈希
- [x] Defender 与 Authenticode 状态记录
- [ ] 本机 v1.3→v1.4 覆盖安装、真实用户库迁移与卸载保留：现有 MCP 进程占用安装目录，本次未强杀，NOT_TESTED
- [ ] Authenticode：三个 EXE 均为 NotSigned，当前无签名证书
- [ ] 真实 Gmail API/IMAP/QQ SMTP E2E：本阶段无网络行为，NOT_TESTED
- [ ] 最终报告、实现 commit 并 push 到 GitHub master：提交推送后勾选

不得自动创建公开 GitHub Release。未签名产物必须明确记录 NotSigned。
