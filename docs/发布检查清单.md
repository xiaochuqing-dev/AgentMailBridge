# v1.4.2 Generic IMAP/SMTP 与 QQ/163 双向收发发布检查清单

- [x] UTF-8 读取阶段提示词，Research Gate 与 Community Research Matrix 完成
- [x] Provider-neutral Incoming/Outgoing Runtime Config 建立，Generic 不再写入 Gmail/QQ shim 字段
- [x] QQ、163、Generic 共用 Generic IMAP/SMTP Core，没有复制收件、发件、归档、调度或重试链
- [x] IMAP initial/incremental、UID overlap、UIDVALIDITY reset、scan cap、批量/逐 UID 降级与单邮件隔离自动化通过
- [x] Generic IMAP 接入立即收取、自动收件、历史补扫、receive rule、retry、Mail Package、Mail Facts 与 MCP ensure_fresh 路由
- [x] SMTP SSL/TLS、STARTTLS、账号级 From、recipient reject 分类、outbound/sent ownership 与既有附件/Hash 链自动化通过
- [x] QQ 与 163 profile、完整 receive/send capability、账号级双凭据槽、GUI 创建/选择与 ownership 自动化通过
- [x] Gmail 收件、OAuth、scheduler、package 和 MCP 回归保持；Gmail scope 仍严格且唯一为 `gmail.readonly`
- [x] Outlook/Microsoft 继续 planned；没有提前扩大 OAuth 或伪装为密码型 Generic
- [x] Multi-Account schema v3 迁移自动化通过；迁移只写账号元数据，不移动归档、不改 raw.eml/Hash
- [x] 一个 MCP、七工具、固定 `OWNER_GMAIL`、路径白名单、UTF-8/stdout purity 与审计边界保持
- [x] 最终冻结代码全量 pytest：564 passed、1 skipped，1562.63 秒
- [x] 重点 v1.4.2/多账号/GUI/迁移回归：45 passed；版本资源修正后定向 2 passed
- [x] compileall、pip check、版本命令与 `git diff --check` 通过
- [x] clean build、GUI packaged self-test、MCP packaged smoke、ZIP、installer、checksums 完成
- [x] 产物版本：GUI、MCP、Setup 的 FileVersion/ProductVersion 均为 1.4.2
- [x] dist/release/ZIP secret scan：309 个文件，0 个配置 secret marker；Git tracked forbidden path 为 0
- [x] Defender release 与 dist CustomScan 完成；检测总数 3→3，无新增
- [ ] QQ 真实账号 IMAP/SMTP/回收/附件 E2E：无独立测试凭据，NOT_TESTED
- [ ] 163 真实账号 IMAP/SMTP/回收/附件 E2E：无独立测试凭据，NOT_TESTED
- [ ] 任意第三方 Generic 真实服务器兼容矩阵：无独立测试账号，NOT_TESTED
- [ ] 真实安装、v1.4.1 覆盖升级、卸载数据保留：缺少隔离 Windows 测试环境，NOT_TESTED
- [ ] 人工 100%/125%/150% 深浅色截图：本阶段未执行，NOT_TESTED
- [ ] Authenticode：GUI、MCP、Setup 均为 NotSigned，当前无签名证书
- [x] 功能、测试与初版报告三个 commits 已正常推送 origin/master；最终状态补记使用独立文档提交继续正常推送

未获得用户明确批准，不得创建 Tag、GitHub Release 或公开 Release Assets。
